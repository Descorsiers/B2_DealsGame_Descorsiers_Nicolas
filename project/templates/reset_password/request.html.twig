{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    {# {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
    <h1>Reset your password</h1>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
        <div>
            <small>
                Enter your email address, and we will send you a
                link to reset your password.
            </small>
        </div>

        <button class="btn btn-primary">Send password reset email</button>
    {{ form_end(requestForm) }} #}
    <section id="formRequest">
        {{ form_start(requestForm, {'attr': {'class': 'formRequest'}}) }}
            <h1>
                Changer de mot de passe 
            </h1>
            <div>
                <div>
                    {{ form_label(requestForm.email, 'Entrer votre email') }}
                    {{ form_widget(requestForm.email, {'attr': {'class': ''}})}}
                </div>
                {{ form_errors(requestForm.email) }}
                {% for flash_error in app.flashes('reset_password_error') %}
                    <small class="" {{ flash_error }}</small>
                {% endfor %}
            </div>
            <button>
                Envoyer demande de changer de mot de passe 
            </button>
        {{ form_end(requestForm)}}    
    </section>
{% endblock %}
